---
phase: 01-preprocessing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hqss_experiment.ipynb
autonomous: true
requirements:
  - PREP-01
  - PREP-02
must_haves:
  truths:
    - "Lambert-W Gaussianization code is completely absent from the notebook"
    - "All variable names and user-facing text use 'growth rate' instead of 'log returns'"
    - "A biological justification markdown cell with the specific growth rate equation exists before the growth rate computation"
    - "Denormalization functions produce growth_rate-scale output without Lambert-W inverse"
    - "VAE DataLoader uses norm_growth_rate instead of gaussianized"
    - "All four models retrain successfully on the new pipeline (classical at full epochs, quantum at reduced epochs)"
  artifacts:
    - path: "hqss_experiment.ipynb"
      provides: "Simplified preprocessing pipeline, updated denorm functions, biological justification cell, renamed variables"
      contains: "growth_rate"
  key_links:
    - from: "cell 4 (preprocessing)"
      to: "cell 11 (DataLoaders)"
      via: "norm_growth_rate variable replaces gaussianized"
      pattern: "norm_growth_rate"
    - from: "cell 7 (denorm functions)"
      to: "cell 14 (generation functions)"
      via: "denorm_qgan_output now takes gr_min, gr_max instead of delta_lw, lw_min, lw_max"
      pattern: "denorm_qgan_output.*gr_min.*gr_max"
    - from: "cell 4 (scaling)"
      to: "cell 7 (denorm)"
      via: "GR_MIN/GR_MAX constants replace LW_MIN/LW_MAX"
      pattern: "GR_MIN|GR_MAX"
---

<objective>
Remove Lambert-W Gaussianization from the preprocessing pipeline, rename all "log returns" references to "specific growth rate" with biological justification, simplify denormalization functions, update all downstream consumers, and retrain all four models on the new pipeline.

Purpose: The Lambert-W transform is a finance technique with no biological justification for OD data. Removing it simplifies the pipeline and strengthens the paper for peer review. The terminology rename aligns with standard microbiology.

Output: A notebook where the preprocessing pipeline is OD -> dither -> specific growth rate -> z-score -> scale[-1,1] (GANs) or z-score only (VAEs), with all four models retrained on the new data.
</objective>

<execution_context>
@/Users/shawngibford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawngibford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-preprocessing/01-CONTEXT.md
@.planning/phases/01-preprocessing/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Lambert-W code, simplify preprocessing and denormalization, rename variables</name>
  <files>hqss_experiment.ipynb</files>
  <read_first>
    - hqss_experiment.ipynb (the only file — read entire notebook to understand current cell structure)
    - .planning/phases/01-preprocessing/01-RESEARCH.md (code examples for replacement patterns)
  </read_first>
  <action>
This task makes all preprocessing pipeline changes in a single pass through the notebook. Work cell-by-cell:

**Cell 2 (imports):** Remove `from scipy.special import lambertw` and `from scipy.optimize import minimize_scalar`. Search remaining cells for `kurtosis` — if only used in the Lambert-W block being deleted, also remove `from scipy.stats import kurtosis`.

**New markdown cell (insert immediately BEFORE the growth rate computation in cell 4):**
```markdown
## Biological Framing: Specific Growth Rate

The core quantity modelled throughout this notebook is the **specific growth rate**:

$$\mu = \frac{d(\ln \text{OD})}{dt}$$

This is the standard microbiological definition of specific growth rate — the instantaneous
fractional rate of increase in biomass (approximated here by optical density). Computing
the first difference of log(OD) yields a discrete approximation of mu at each measurement
interval.
```

**Cell 4 (preprocessing):**
1. Rename `log_delta` variable to `growth_rate` everywhere in this cell
2. Rename `norm_log_delta` to `norm_growth_rate`
3. Delete the entire Lambert-W block: `_kurtosis_for_delta()` function, `minimize_scalar()` call, `DELTA_LW` constant, `lambert_w_gaussianize()` function, `lambert_w_inverse()` function, `gaussianized` variable assignment
4. Delete constants `LW_MIN` and `LW_MAX`
5. Replace the scaling block with direct scaling from z-scored growth rate:
```python
GR_MIN = float(norm_growth_rate.min())
GR_MAX = float(norm_growth_rate.max())
scaled_data = (-1.0 + 2.0 * (norm_growth_rate - GR_MIN) / (GR_MAX - GR_MIN)).astype(np.float32)
print(f'Scaled [-1,1]: mean={scaled_data.mean():.4f}, std={scaled_data.std():.4f}')
print(f'Z-score range: [{GR_MIN:.4f}, {GR_MAX:.4f}]')
```
6. Update MU/SIGMA print statements to reference "growth rate" instead of "log returns"
7. Keep dithering step unchanged

**Cell 5 (diagnostic plots):** Update all string literals:
- `'Log Return'` -> `'Specific Growth Rate'` (or `'Growth Rate'`)
- `'Log Returns Time Series'` -> `'Specific Growth Rate Time Series'`
- `'Log Returns Distribution'` -> `'Growth Rate Distribution'`
- Update Q-Q plot title similarly
- Update any axis labels referencing log_delta or log returns

**Cell 7 (denormalization functions):** Rewrite both denorm functions:
```python
def denorm_qgan_output(gen_windows, mu, sigma, gr_min, gr_max):
    """Reverse qGAN preprocessing: [-1,1] -> z-score -> growth_rate."""
    flat = gen_windows.reshape(-1).detach().float().numpy()
    unscaled = 0.5 * (flat + 1.0) * (gr_max - gr_min) + gr_min
    return (unscaled * sigma + mu).astype(np.float32)

def denorm_vae_output(windows_np, mu, sigma):
    """Reverse VAE preprocessing: z-score -> growth_rate."""
    flat = windows_np.reshape(-1).astype(np.float64)
    return (flat * sigma + mu).astype(np.float32)
```
Remove the `lambert_w_inverse` function if it was defined here (it may be in cell 4 — delete from wherever it is).

**Cell 11 (DataLoaders):** Change VAE windowing source:
```python
# FROM: vae_windows = _sliding_windows(gaussianized, WINDOW_VAE, stride=STRIDE_VAE)
# TO:
vae_windows = _sliding_windows(norm_growth_rate, WINDOW_VAE, stride=STRIDE_VAE)
```

**Cell 14 (generation functions):** Update all callers of denorm functions:
- `denorm_qgan_output(gen_windows, MU, SIGMA, DELTA_LW, LW_MIN, LW_MAX)` -> `denorm_qgan_output(gen_windows, MU, SIGMA, GR_MIN, GR_MAX)`
- `denorm_vae_output` calls: remove any `DELTA_LW` argument if present
- Rename any local variables from `log_delta` to `growth_rate`
- Update the `log_delta_to_od()` function name or its internal variable references — if the function is called with growth_rate data, update the docstring/comments to reflect that

**Cell 15 (MODELS_INFO):** Rename any `log_delta` references in generation output handling

**Cell 39 (IBM generation):** Update `_qgan_generate_one_ibm` to use new denorm signature: `denorm_qgan_output(gen_windows, MU, SIGMA, GR_MIN, GR_MAX)`. Remove any `DELTA_LW`, `LW_MIN`, `LW_MAX` references.

**All remaining cells (18-27+):** Search for and replace:
- All instances of `log_delta` variable name -> `growth_rate`
- All string literals containing "log return" (case-insensitive) -> "specific growth rate" or "growth rate"
- All references to `gaussianized` -> `norm_growth_rate` (should be none after cell 4/11 fix, but verify)
- All references to `DELTA_LW`, `LW_MIN`, `LW_MAX` -> remove or replace with `GR_MIN`, `GR_MAX`

**Training cells (force retrain):** Set all force-retrain flags to True:
```python
FORCE_TRAIN_QGAN = True
FORCE_TRAIN_QVAE = True
FORCE_TRAIN_CGAN = True
FORCE_TRAIN_CVAE = True
```

Add reduced epoch logic for quantum models:
```python
PHASE1_REDUCED = True
QGAN_EPOCHS_PHASE1 = 100  # reduced from full 500
QVAE_EPOCHS_PHASE1 = 20   # reduced from full 50
```
In the qGAN training cell, use `QGAN_EPOCHS_PHASE1 if PHASE1_REDUCED else QGAN_EPOCHS` for epoch count. Same pattern for qVAE with `QVAE_EPOCHS_PHASE1`.

**Final sweep:** Search the entire notebook JSON source for these strings that must be ABSENT after edits:
- `lambert_w` (function names, variable references)
- `DELTA_LW`
- `LW_MIN`
- `LW_MAX`
- `gaussianized` (variable name)
- `log_delta` (variable name — note: `log_delta_to_od` function name may remain if it still converts growth rates to OD, but rename if practical)
- `log return` / `Log Return` / `log returns` / `Log Returns` (string literals)
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/HQSS && python3 -c "
import json
with open('hqss_experiment.ipynb') as f:
    nb = json.load(f)
src = ''.join(line for cell in nb['cells'] for line in cell['source'])
# Must be absent
for term in ['lambert_w', 'DELTA_LW', 'LW_MIN', 'LW_MAX', 'gaussianized']:
    assert term not in src, f'FAIL: {term} still in notebook'
# Must be present
for term in ['growth_rate', 'GR_MIN', 'GR_MAX', 'norm_growth_rate', 'PHASE1_REDUCED', 'denorm_qgan_output', 'denorm_vae_output']:
    assert term in src, f'FAIL: {term} missing from notebook'
# Check bio justification cell
bio_cells = [c for c in nb['cells'] if c['cell_type']=='markdown' and 'specific growth rate' in ''.join(c['source']).lower()]
assert len(bio_cells) >= 1, 'FAIL: no biological justification markdown cell found'
# Check log_delta is gone from variable names (allow in comments/strings about the transformation)
import re
code_src = ''.join(line for cell in nb['cells'] if cell['cell_type']=='code' for line in cell['source'])
# Check no log_delta as a variable assignment or usage (not in strings)
matches = re.findall(r'(?<![\"\'\\w])log_delta(?![\"\'\\w])', code_src)
assert len(matches) == 0, f'FAIL: log_delta still used as variable name ({len(matches)} occurrences)'
print('ALL CHECKS PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Notebook source contains zero occurrences of `lambert_w`, `DELTA_LW`, `LW_MIN`, `LW_MAX`, `gaussianized`
    - Notebook source contains `growth_rate`, `norm_growth_rate`, `GR_MIN`, `GR_MAX`
    - Notebook source contains `PHASE1_REDUCED = True`
    - A markdown cell containing "specific growth rate" and the equation `d(\ln` exists
    - `denorm_qgan_output` function signature contains `gr_min, gr_max` (not `lw_min, lw_max` or `delta_lw`)
    - `denorm_vae_output` function signature does NOT contain `delta_lw`
    - `vae_windows` line references `norm_growth_rate` (not `gaussianized`)
    - `log_delta` does not appear as a variable name in any code cell
    - All force-retrain flags are set to `True`
    - `QGAN_EPOCHS_PHASE1 = 100` and `QVAE_EPOCHS_PHASE1 = 20` are present
  </acceptance_criteria>
  <done>The preprocessing pipeline is simplified to OD -> dither -> growth_rate -> z-score -> scale[-1,1] (GANs) / z-score (VAEs). All Lambert-W code is deleted. All "log returns" text is replaced with "specific growth rate". A biological justification cell with the mu equation is present. Denorm functions are rewritten without Lambert-W inverse. All downstream cells reference the new variable names and constants. Force-retrain flags are set and reduced epoch counts are configured for quantum models.</done>
</task>

<task type="auto">
  <name>Task 2: Verify notebook executes through preprocessing and classical model training</name>
  <files>hqss_experiment.ipynb</files>
  <read_first>
    - hqss_experiment.ipynb (verify the changes from Task 1 are in place)
  </read_first>
  <action>
Run the notebook cells sequentially through the preprocessing and classical model training to verify no NameErrors or shape mismatches:

1. Execute cells 0-5 (config, imports, data loading, preprocessing, diagnostics). Verify:
   - No `NameError` for removed variables
   - `growth_rate` array has shape (777,)
   - `norm_growth_rate` array has same shape
   - `scaled_data` array has values in [-1, 1]
   - `GR_MIN` and `GR_MAX` are printed and are finite floats
   - Diagnostic plot renders without "log return" labels

2. Execute cell 7 (denorm functions). Verify functions are defined without Lambert-W references.

3. Execute cell 11 (DataLoaders). Verify:
   - `gan_loader` produces batches without error
   - `vae_loader` produces batches without error (sources from `norm_growth_rate`)

4. Execute classical model training cells (cGAN cell 9-10, cVAE cell 10). Verify:
   - cGAN trains without error at full epochs
   - cVAE trains without error at full epochs
   - Checkpoints are saved to `checkpoints/models/`

5. Execute cell 14-15 (generation functions + MODELS_INFO). Verify:
   - Generation functions reference `GR_MIN`, `GR_MAX` instead of Lambert-W constants
   - A quick single-window generation test for cGAN and cVAE produces non-NaN output

If any cell fails with NameError, TypeError, or shape mismatch, fix the issue in-place and re-run.

Note: Do NOT run quantum model training (qGAN, qVAE) in this verification — it takes too long. Only verify the cells parse without error by checking the function definitions exist.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/HQSS && python3 -c "
import json
with open('hqss_experiment.ipynb') as f:
    nb = json.load(f)
# Verify no lambert_w references remain
src = ''.join(line for cell in nb['cells'] for line in cell['source'])
assert 'lambert_w' not in src, 'lambert_w still present'
assert 'DELTA_LW' not in src, 'DELTA_LW still present'
assert 'GR_MIN' in src, 'GR_MIN missing'
assert 'GR_MAX' in src, 'GR_MAX missing'
assert 'FORCE_TRAIN_CGAN = True' in src or 'FORCE_TRAIN_CGAN=True' in src, 'FORCE_TRAIN_CGAN not True'
print('NOTEBOOK STRUCTURE VERIFIED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Cells 0-5 execute without NameError or ValueError
    - `growth_rate` shape is (777,) as printed by cell 4
    - `scaled_data` values are within [-1.0, 1.0]
    - `vae_loader` iterates without NameError (no reference to `gaussianized`)
    - cGAN and cVAE training cells complete without error
    - Generation functions for cGAN and cVAE produce finite (non-NaN) output arrays
    - No cell output contains the string "log return" (case-insensitive)
  </acceptance_criteria>
  <done>The notebook preprocessing pipeline, denormalization functions, DataLoaders, classical model training, and generation functions all execute correctly on the new Lambert-W-free pipeline with renamed variables. Any runtime errors discovered during verification have been fixed.</done>
</task>

</tasks>

<verification>
1. Search notebook JSON source for forbidden terms (lambert_w, DELTA_LW, LW_MIN, LW_MAX, gaussianized, log_delta as variable, "log return" in strings) — all must be absent
2. Search notebook JSON source for required terms (growth_rate, norm_growth_rate, GR_MIN, GR_MAX, PHASE1_REDUCED, biological justification markdown cell) — all must be present
3. Classical model training and generation produces valid output on the simplified pipeline
</verification>

<success_criteria>
- Lambert-W code is completely removed (zero occurrences of lambert_w, DELTA_LW, LW_MIN, LW_MAX, gaussianized)
- All "log returns" references replaced with "specific growth rate" / "growth rate"
- Biological justification markdown cell present with mu = d(ln OD)/dt equation
- Denormalization chain simplified (no Lambert-W inverse step)
- VAE DataLoader sources from norm_growth_rate
- Force-retrain flags set to True with reduced quantum epochs configured
- Classical models train and generate successfully on new pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/01-preprocessing/01-01-SUMMARY.md`
</output>
